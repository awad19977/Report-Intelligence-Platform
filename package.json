{
  "name": "report-intelligence-platform",
  "version": "0.1.0",
  "private": true,
  "license": "LGPL-3.0-only",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "npm run build --workspace=@rip/core && npm run build --workspace=@rip/crystal-client && npm run build --workspace=@rip/plugin-api && npm run build --workspace=@rip/grpc-transport && npm run build --workspace=@rip/sdk",
    "build:apps": "npm run build --workspace=@rip/cli && npm run build --workspace=@rip/mcp-server",
    "dev": "npm run dev --workspace=apps/mcp-server",
    "typecheck": "tsc --noEmit --project tsconfig.base.json",
    "test": "npm test --workspace=@rip/crystal-client && npm test --workspace=@rip/mcp-server",
    "extract:real-report": "node scripts/extract-real-report.mjs",
    "clean": "rimraf -g \"packages/*/dist\" \"apps/*/dist\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
