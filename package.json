{
  "name": "report-intelligence-platform",
  "version": "0.1.0",
  "private": true,
  "license": "LGPL-3.0-only",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:packages": "npm run build --workspace=@report-intelligence/core && npm run build --workspace=@report-intelligence/crystal-client && npm run build --workspace=@report-intelligence/plugin-api && npm run build --workspace=@report-intelligence/grpc-transport && npm run build --workspace=@report-intelligence/sdk",
    "build:apps": "npm run build --workspace=@report-intelligence/cli && npm run build --workspace=@report-intelligence/mcp-server",
    "dev": "npm run dev --workspace=apps/mcp-server",
    "typecheck": "tsc --noEmit --project tsconfig.base.json",
    "test": "npm test --workspace=@report-intelligence/crystal-client && npm test --workspace=@report-intelligence/mcp-server",
    "extract:real-report": "node scripts/extract-real-report.mjs",
    "clean": "rimraf -g \"packages/*/dist\" \"apps/*/dist\""
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.0",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
